<div class="container">

  <h1 class="mt-3">
    <%= @room.name %>
  </h1>

  <div id="tickets" data-room-id="<%= @room.id %>">
    <%= render partial: 'tickets', locals: {room: @room, tickets: @tickets} %>
  </div>

</div>