<div class="container">
  <div class="row justify-content-center">
    <div class="col-4">
      <h1>New Room</h1>
      <%= simple_form_for @room do |f| %>
        <%= f.input :name %>
        <%= f.submit 'Create Room', class: 'btn btn-primary btn-block' %>
      <% end %>
    </div>
  </div>
</div>